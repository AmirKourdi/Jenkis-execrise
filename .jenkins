pipeline {
    agent any
    stages {
        stages('Build'){
            steps {
               sh 'echo "Hellow World"'
               sh '''
                   echo "Multiline shell steps works too"
                   ls -lah
               '''
            }
        }
    } 
}
